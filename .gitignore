# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Web related
lib/generated_plugin_registrant.dart

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# iOS/XCode related
**/ios/**/*.mode1v3
**/ios/**/*.mode2v3
**/ios/**/*.moved-aside
**/ios/**/*.pbxuser
**/ios/**/*.perspectivev3
**/ios/**/*sync/
**/ios/**/.sconsign.dblite
**/ios/**/.tags*
**/ios/**/.vagrant/
**/ios/**/DerivedData/
**/ios/**/Icon?
**/ios/**/Pods/
**/ios/**/.symlinks/
**/ios/**/profile
**/ios/**/xcuserdata
**/ios/.generated/
**/ios/Flutter/App.framework
**/ios/Flutter/Flutter.framework
**/ios/Flutter/Flutter.podspec
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/app.flx
**/ios/Flutter/app.zip
**/ios/Flutter/flutter_assets/
**/ios/Flutter/flutter_export_environment.sh
**/ios/ServiceDefinitions.json
**/ios/Runner/GeneratedPluginRegistrant.*

# macOS system files
.DS_Store
**/.DS_Store

# Duplicate files
* (1)*
* (2)*
* (3)*

# Coverage
coverage/

# Exceptions to above rules.
!**/ios/**/default.mode1v3
!**/ios/**/default.mode2v3
!**/ios/**/default.pbxuser
!**/ios/**/default.perspectivev3
!/packages/flutter_tools/test/data/dart_dependencies_test/**/.packages

# FVM
.fvm/flutter_sdk

# Firebase
# google-services.json / GoogleService-Info.plist are ignored since 2026-09-12.
# Their API key values also ship inside every binary, so this does not make them
# secret; it keeps them out of a public repo that is scanned automatically. The
# build needs them, so after a fresh clone download both from the Firebase console
# (Project settings > Your apps). `git rm --cached` leaves the working copy alone,
# so an existing checkout keeps building.
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
**/firebase_app_id_file.json

# Environment files
.env
.env.local
.env.production
.env.staging

# Generated files
*.g.dart
*.freezed.dart
# firebase_options.dart is ignored since 2026-09-12. This does not make its API
# keys secret: the same values sit in google-services.json / GoogleService-Info.plist
# and inside every shipped binary. What it does is keep them out of the file that
# GitHub's scanner flags. Pushing this file on 2026-09-06 was detected, reported to
# Google through the secret scanning partner program, and the project was suspended
# two days later. Run `flutterfire configure` after a fresh clone to regenerate it.
lib/firebase_options.dart

# Anything regenerable stays out (2026-09-19). `flutterfire configure` and the
# Firebase console write these back, so tracking them only publishes the
# project identifiers into a repository that is scanned automatically.
firebase.json

# AdMob and Firebase IDs
# Not games-ids.xml: the other four apps that use Play Games track theirs.
# Excluding it here is why this app lost the file. The Play Games app id
# ships inside the binary, the same as the AdMob app id next to it.

# Not ios/Runner/Info.plist: the build needs it. It was excluded, never
# tracked, and eventually lost; it had to be rebuilt by hand on 2026-09-02.
# The AdMob app id it holds ships inside every copy of the app anyway.

# Build artifacts and cache
**/.cxx/
# Not **/gradle/: that is where the Gradle wrapper lives. Only its cache
# directory should be hidden.
**/.gradle/
**/build/
**/GeneratedPluginRegistrant.*

# Not lib/l10n/app_localizations*.dart: regenerated from the .arb files, but
# the other five apps with an l10n.yaml track theirs. One app differing is
# what turns into a question later.

# Not **/AndroidManifest.xml: the Android build cannot start without it. It
# was excluded, never tracked, and lost; rebuilt by hand on 2026-09-02.

# --- Tooling config that belongs to the workspace, not to this app ---
.claude/
.cursorignore

# --- Flutter iOS build output (SPM checkouts, generated env) ---
ios/Flutter/ephemeral/

# --- Build output and generated plugin registrants (regenerated by pub get) ---
.flutter-plugins
.flutter-plugins-dependencies
ios/Podfile.lock
ios/.symlinks/
ios/Flutter/.last_build_id
ios/build/
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
**/generated_plugin_registrant.*
**/xcuserdata/

# --- Signing assets (added 2026-09-06) ---
# None of these are in any repository today. The rules exist so that a
# certificate exported to the project directory for a one-off cannot be
# committed by a later `git add -A`. Distribution certificates and the App
# Store Connect key grant publishing rights, and a leak is not fixed by
# deleting the file: the history keeps it.
*.p12
*.p8
*.cer
*.certSigningRequest
*.mobileprovision
*.provisionprofile
*.jks
*.keystore

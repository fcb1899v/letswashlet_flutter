# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Web related
lib/generated_plugin_registrant.dart

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# iOS/XCode related
**/ios/**/*.mode1v3
**/ios/**/*.mode2v3
**/ios/**/*.moved-aside
**/ios/**/*.pbxuser
**/ios/**/*.perspectivev3
**/ios/**/*sync/
**/ios/**/.sconsign.dblite
**/ios/**/.tags*
**/ios/**/.vagrant/
**/ios/**/DerivedData/
**/ios/**/Icon?
**/ios/**/Pods/
**/ios/**/.symlinks/
**/ios/**/profile
**/ios/**/xcuserdata
**/ios/.generated/
**/ios/Flutter/App.framework
**/ios/Flutter/Flutter.framework
**/ios/Flutter/Flutter.podspec
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/app.flx
**/ios/Flutter/app.zip
**/ios/Flutter/flutter_assets/
**/ios/Flutter/flutter_export_environment.sh
**/ios/ServiceDefinitions.json
**/ios/Runner/GeneratedPluginRegistrant.*

# macOS system files
.DS_Store
**/.DS_Store

# Duplicate files
* (1)*
* (2)*
* (3)*

# Coverage
coverage/

# Exceptions to above rules.
!**/ios/**/default.mode1v3
!**/ios/**/default.mode2v3
!**/ios/**/default.pbxuser
!**/ios/**/default.perspectivev3
!/packages/flutter_tools/test/data/dart_dependencies_test/**/.packages

# FVM
.fvm/flutter_sdk

# Firebase
# Not google-services.json / GoogleService-Info.plist: this app applies the
# com.google.gms.google-services Gradle plugin, which fails the Android build
# when the json is absent, and six of the eight Xcode projects list the plist in
# their Resources build phase. Both files hold the same identifiers as
# lib/firebase_options.dart and ship inside the app, so excluding them hid
# nothing and cost two apps their copy (kawaii and letswashlet, 2026-09-02).
**/firebase_app_id_file.json

# Environment files
.env
.env.local
.env.production
.env.staging

# Generated files
*.g.dart
*.freezed.dart
# Not firebase_options.dart: the build needs it, and every value in it
# (apiKey, appId, projectId, messagingSenderId, storageBucket, and the
# iOS/Android client ids) ships inside the app binary, so hiding the file
# hides nothing from anyone who has the app. A Firebase API key identifies
# the project; it does not authorize access. Firestore rules and App Check
# do that.

# AdMob and Firebase IDs
# Not games-ids.xml: the other four apps that use Play Games track theirs.
# Excluding it here is why this app lost the file. The Play Games app id
# ships inside the binary, the same as the AdMob app id next to it.

# Not ios/Runner/Info.plist: the build needs it. It was excluded, never
# tracked, and eventually lost; it had to be rebuilt by hand on 2026-09-02.
# The AdMob app id it holds ships inside every copy of the app anyway.

# Build artifacts and cache
**/.cxx/
# Not **/gradle/: that is where the Gradle wrapper lives. Only its cache
# directory should be hidden.
**/.gradle/
**/build/
**/GeneratedPluginRegistrant.*

# Not lib/l10n/app_localizations*.dart: regenerated from the .arb files, but
# the other five apps with an l10n.yaml track theirs. One app differing is
# what turns into a question later.

# Not **/AndroidManifest.xml: the Android build cannot start without it. It
# was excluded, never tracked, and lost; rebuilt by hand on 2026-09-02.

# --- Tooling config that belongs to the workspace, not to this app ---
.claude/
.cursorignore

# --- Flutter iOS build output (SPM checkouts, generated env) ---
ios/Flutter/ephemeral/

# --- Build output and generated plugin registrants (regenerated by pub get) ---
.flutter-plugins
.flutter-plugins-dependencies
ios/Podfile.lock
ios/.symlinks/
ios/Flutter/.last_build_id
ios/build/
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
**/generated_plugin_registrant.*
**/xcuserdata/

# --- Signing assets (added 2026-09-06) ---
# None of these are in any repository today. The rules exist so that a
# certificate exported to the project directory for a one-off cannot be
# committed by a later `git add -A`. Distribution certificates and the App
# Store Connect key grant publishing rights, and a leak is not fixed by
# deleting the file: the history keeps it.
*.p12
*.p8
*.cer
*.certSigningRequest
*.mobileprovision
*.provisionprofile
*.jks
*.keystore
